'use client';

import { AppLayout } from '@/components/app-layout';

export default function GeneticPage() {
  return (
    <AppLayout>
      <div className="max-w-7xl mx-auto">
        <h1 className="text-3xl font-bold mb-6">基因洞察</h1>
        <p className="text-muted-foreground">基因标记追踪功能即将上线...</p>
      </div>
    </AppLayout>
  );
}
